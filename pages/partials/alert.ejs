<!-- data-disabled was here -->
<div class="alert-box"> 
   <% if (typeof data.alert !== 'undefined' && data.alert) { %>
    <div data-info>
        <% Object.entries(data.alert).forEach(([key, value]) => { %>
            <p id="<%= key %>"><%= value %></p>
        <% }) %>
    </div>
   <% } %>
</div>